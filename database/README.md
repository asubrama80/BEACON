# database/

This directory holds BEACON's PostgreSQL schema, Drizzle ORM migrations, and seed data.

**Module 00 scope:** none. PostgreSQL is provisioned as a Docker development service only (see [docker-compose.yml](../docker-compose.yml)); no application tables or Drizzle configuration exist yet.

Schema and migrations are introduced in **Module 01 — Database Foundation**.
