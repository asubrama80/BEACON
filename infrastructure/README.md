# infrastructure/

This directory holds deployment infrastructure that goes beyond local development: production Nginx routing/TLS, Docker Compose production overlays, and related deployment configuration.

**Module 00 scope:** none. Local development orchestration (frontend, backend, PostgreSQL) is defined in the root [docker-compose.yml](../docker-compose.yml). Production deployment is introduced in later modules (Docker Deployment, Production Readiness).
