# tests/

This directory is reserved for cross-cutting integration and end-to-end tests that span the frontend, backend, and database together.

**Module 00 scope:** none. Module 00's unit-level tests (backend `/health` test, frontend smoke test) live alongside their source in `backend/src` and `frontend/src`. Cross-cutting tests are added as later modules introduce the flows they exercise.
